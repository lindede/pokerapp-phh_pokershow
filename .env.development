# 开发时 Vite 把浏览器请求 /v1、/v2 转发到这里（见 vite.config.ts）
VITE_COMMENTARY_DEV_PROXY_TARGET=http://127.0.0.1:9000
# 语音 list/data 默认走线上（本地 9000 通常无 voice 路由）
# VITE_VOICE_DEV_PROXY_TARGET=https://www.pokershow.top
